'use strict';

require('../graph-class/graph');

class BreadthFirst extends Graph {

  // the breadth first traversal takes the starting node as its argument
  // it stores an object containing the visited nodes
  // it also stores anand an array of values listing the visited nodes in order

  breadthFirst(node) {
    let visitedNodesArray = [];

  }
}
